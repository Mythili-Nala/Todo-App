# Todo-App
Rest API 

Building a simple API for Todo application with Golang. 

Frameworks:
Gin-gonic 
Gorm for database 

To run in localhost,

POST todos/
GET todos/
GET todos/{id}
PUT todos/{id}
DELETE todos/{id}
